using UnityEngine;

public interface IColorAnimator {

    void SetBaseColor(Color color);
    void SetColor(Color color);
    void SetHueOffset
    
    (float offset); // sets a hue offset from the original


 }
